namespace ZXTemplate.Settings
{
    /// <summary>
    /// Save keys used by SettingsService with ISaveService.
    /// Keep these stable to avoid breaking existing saves.
    /// </summary>
    public static class SettingsKeys
    {
        public const string Main = "settings_main";
    }
}
