namespace ZXTemplate.Progress
{
    /// <summary>
    /// Save keys used by ProgressService with ISaveService.
    /// Keep these stable to avoid breaking existing saves.
    /// </summary>
    public static class ProgressKeys
    {
        public const string Main = "progress_main";
    }
}
